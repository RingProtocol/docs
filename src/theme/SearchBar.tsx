import OriginalSearchBar from '@theme-original/SearchBar'
import React from 'react'

export default function SearchBar(props: React.ComponentProps<typeof OriginalSearchBar>) {
  return <OriginalSearchBar {...props} />
}
